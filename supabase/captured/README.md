# Captured live Supabase database metadata

Read-only capture generated 2026-07-12 from the temporary `audit_readonly` PostgreSQL role. No DDL or data mutation was executed. CSV files cover schemas `public`, `auth`, and `storage`; publication files cover all publications. `schema.sql` is intentionally absent because local `pg_dump` 16 cannot dump the PostgreSQL 17.6 server. Structured catalog exports are the authoritative capture.
